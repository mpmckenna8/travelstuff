import React from 'react'

// visible itemlist to go here interacting with reducer
const VisibleItemList = ({params}) => {
  return (
    <div>make all the items appear here</div>
  )
}

export default VisibleItemList
